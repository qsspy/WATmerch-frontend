import { ProductDetailsModel } from './product-details-model';

describe('ProductDetailsModel', () => {
  it('should create an instance', () => {
    expect(new ProductDetailsModel()).toBeTruthy();
  });
});
