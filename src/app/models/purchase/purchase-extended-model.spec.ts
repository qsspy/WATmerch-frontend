import { PurchaseExtendedModel } from './purchase-extended-model';

describe('PurchaseExtendedModel', () => {
  it('should create an instance', () => {
    expect(new PurchaseExtendedModel()).toBeTruthy();
  });
});
