import { OrderProductApiModel } from './order-product-api-model';

describe('OrderProductApiModel', () => {
  it('should create an instance', () => {
    expect(new OrderProductApiModel()).toBeTruthy();
  });
});
